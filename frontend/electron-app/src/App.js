import React from 'react';
import Game from './components/Game'

// App.js is the main component of your React application.
// It acts as the root component where you define the structure and content of your app.
// Core of applications UI!

const App = () => {
    return <Game />;
};

export default App;