
  "scripts": {
    "start": "electron .",
    "dev": "webpack-dev-server --open",
    "build": "webpack --mode production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },


npm run start, npm run dev, npm run <define>
Those are the standard way you can run/boot your client!
